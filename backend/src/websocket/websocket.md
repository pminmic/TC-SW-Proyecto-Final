# Websocket Module

In this module, is `broadcaster.rs` which contains the handlers for the websocket connections. It is responsible for broadcasting messages to all connected clients, separating the data messages and the information messages.